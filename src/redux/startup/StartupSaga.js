// import { put, select, takeEvery } from 'redux-saga/effects'
// import { REHYDRATED } from './index'
// import { changeLocale } from '../settings'

// const selectLocale = state => state.settings.locale

// const startupLocale = function* (action) {
//   // const locale = yield select(selectLocale)
//   // // Always set the I18n locale to the language in the settings, or the views would render in the language of the device's locale and not that of the setting.
//   // yield put(changeLocale(locale))
// }

// export default function* () {
//   //yield takeEvery(REHYDRATED, startupLocale)
// }